const defaultStyles =
  "border-gray-300 bg-white text-gray-700 hover:bg-gray-50 focus:ring-primary-500 font-medium"
const primaryStyles =
  "border-primary-600 text-white bg-primary-600 hover:bg-primary-600 hover:text-white focus:ring-primary-500 font-semibold"

const commonStyles = `inline-flex items-center rounded-md border px-3 py-1 text-md shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2`

export const defaultButtonStyles = `${commonStyles} ${defaultStyles}`
export const primaryButtonStyles = `${commonStyles} ${primaryStyles}`
